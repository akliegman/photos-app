import { createSelector } from "reselect";

// export const getAllLoadingStates = createSelector(
//   (state: any) => state,
//   (state) => {
//     // map all loading states and set loading to true if any of them is true
//     const loadingStates = Object.keys(state).map((key) => state[key].loading);
//     return {
//       loading: loadingStates.some((loadingState) => loadingState === true),
//     };
//   }
// );
